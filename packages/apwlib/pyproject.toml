[project]
name = "apwlib"
version = "0.1.0"
description = "Core library for apwcli."
readme = "README.md"
license = "MIT"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["uv_build>=0.8.22,<0.9"]
build-backend = "uv_build"
